# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
R:
  - release

cache: packages

addons:
  apt:
    packages:
      - libeccodes-tools
      - gdal-bin
      - libgdal-dev
